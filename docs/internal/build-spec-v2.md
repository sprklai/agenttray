# Archived Build Spec

This file used to be a long planning document. It is kept only as a placeholder so old links do not break.

It is not a source of truth for the current repo state.

It described features that changed, moved, or never shipped, so keeping the original version here would be more confusing than helpful.

Use these instead:

- `README.md` for the user-facing overview
- `docs/compatibility.md` for current support status
- `CONTRIBUTING.md` for contribution paths
- `scripts/README.md` plus the code under `scripts/` and `src-tauri/src/` for implementation details

If the old planning doc is ever needed for historical reference, it can be recovered from git history.
